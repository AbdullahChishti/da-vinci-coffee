{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm run dev:web",
    "dev:web": "pnpm --filter @workspace/da-vinci run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "build:pages": "pnpm --filter @workspace/da-vinci run build",
    "deploy:pages": "pnpm run build:pages && dotenv -e .env -- wrangler pages deploy artifacts/da-vinci/dist/public --project-name=da-vinci-coffee --branch=main",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "wrangler": "^4"
  }
}
